Para correrlo 
abrir una terminal en el directorio del proyecto

crear un entorno virtual python (en windows saltar esto)
source venv/bin/activate

instalar streamlit con 
pip instal streamlit

correr:
streamlit run app.py
